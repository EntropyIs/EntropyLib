#pragma once
#include <cmath>;

const float PI = acos(-1.0f);
const float SQRT2OVER2 = sqrt(2.0f) / 2.0f;
const float SQRT3OVER2 = sqrt(3.0f) / 2.0f;

bool FLOAT_EQ(float left, float right, float threshold = 0.0000001);
